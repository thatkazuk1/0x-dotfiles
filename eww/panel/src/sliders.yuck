(deflisten slider_data :initial '{"volume": 0, "brightness": 0}' "bash ~/.config/eww/panel/scripts/sliders.sh")

(defwidget widget_sliders []
  (box :orientation "vertical"
    :space-evenly false
    :spacing 16
    :class "sliders-container"

    (box :class "slider-wrapper"
      :orientation "vertical"
      :space-evenly false
      :spacing 8

      (label :class "slider-title"
        :text "Brightness"
      :halign "start")

      (box :orientation "horizontal"
        :space-evenly false
        :spacing 12
        :hexpand true

        (label :class "slider-icon brightness"
        :text "󰖨")
        (scale :class "osd-scale brightness"
          :hexpand false
          :width 300
          :valign "center"
          :min 0
          :max 101
          :value {slider_data.brightness}
        :onchange "brightnessctl -q set {}%")
      )
    )

    (box :class "slider-wrapper"
      :orientation "vertical"
      :space-evenly false
      :spacing 8

      (label :class "slider-title"
        :text "Volume"
      :halign "start")

      (box :orientation "horizontal"
        :space-evenly false
        :spacing 12
        :hexpand true

        (label :class "slider-icon volume"
        :text "")
        (scale :class "osd-scale volume"
          :hexpand false
          :width 300
          :valign "center"
          :min 0
          :max 101
          :value {slider_data.volume}
        :onchange "wpctl set-volume -l 1 @DEFAULT_AUDIO_SINK@ {}%")
      )
    )
  )
)
